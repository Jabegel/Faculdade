#include <stdio.h>
int main() {
int contador;
// Este loop imprime os números de 1 a 10
for (contador = 1; contador <= 10;
contador++) {
printf("%d ", contador);
}
printf("\n"); // Para pular para a próxima linha
return 0;
}